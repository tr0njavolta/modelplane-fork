apiVersion: modelplane.ai/v1alpha1
kind: ModelReplica
metadata:
  name: llama405b-h100-cluster
  namespace: ml-team
spec:
  modelRef:
    name: llama-405b
  inferenceClusterRef:
    name: h100-cluster
  scaling:
    signal: Fixed
    fixed:
      replicas: 1
