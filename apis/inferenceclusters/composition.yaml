apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: inferenceclusters.modelplane.ai
spec:
  compositeTypeRef:
    apiVersion: modelplane.ai/v1alpha1
    kind: InferenceCluster
  mode: Pipeline
  pipeline:
  - functionRef:
      name: modelplane-modelplanecompose-inference-cluster
    step: compose-inference-cluster
