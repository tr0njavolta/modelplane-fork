apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: modelreplicas.modelplane.ai
spec:
  compositeTypeRef:
    apiVersion: modelplane.ai/v1alpha1
    kind: ModelReplica
  mode: Pipeline
  pipeline:
  - functionRef:
      name: modelplane-modelplanecompose-model-replica
    step: compose-model-replica
